apiVersion: v1
kind: Service
metadata:
  name: env-healing-agents
  namespace: env-healing-agents-ns
  labels:
    app: env-healing-agents
spec:
  type: ClusterIP
  selector:
    app: env-healing-agents
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
